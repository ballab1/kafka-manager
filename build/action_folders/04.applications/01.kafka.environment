# create property file for runtime
cat << EOF > ${CRF['env']}/kafka.properties
#!/bin/bash

export KM_ARGS="\${KM_ARGS:-${KM_ARGS:-'-Djava.net.preferIPv4Stack=true'}}"
export KM_CONTEXT="\${KM_CONTEXT-${KM_CONTEXT:-/}}"
export KM_VERSION="1.3.3.16"

EOF
