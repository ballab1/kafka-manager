# kafka-manager build Packages

declare -a OPTS=( '--virtual' '.buildDepedencies' )
declare -a PKGS=( git 
                  gnupg 
                  tar 
                  unzip 
                  wget )
